package treehou.se.habit.util;

/**
 * Created by ibaton on 2014-09-10.
 */
public class Loader {

    /*public static List<Server> loadServers(Context context){

        List<Server> servers = Server.

        SharedPreferences preferences = context.getSharedPreferences(Constants.PREFERENCE_SERVER, Context.MODE_PRIVATE);

        Server server = new Server();
        server.setUsername(preferences.getString(Constants.PREF_USERNAME,null));
        server.setPassword(preferences.getString(Constants.PREF_PASSWORD,null));
        server.setLocalUrl(preferences.getString(Constants.PREF_URL_LOCAL,null));
        server.setRemoteUrl(preferences.getString(Constants.PREF_URL_REMOTE,null));

        if(server.getLocalUrl() == null){
            return null;
        }

        return server;
    }*/
}
